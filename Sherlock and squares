def squares(a, b):
    count = 0
    if a <= 1 or b<=1:
        return 0
    else:
        for i in range(a,b+1):
            print(i**(1/2))
            if (i**(1/2)).is_integer():
                count += 1
        return count

            
print(squares)  
